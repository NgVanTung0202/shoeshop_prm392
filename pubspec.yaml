name: shoeshop_prm392
description: Shoe Shop PRM392 Flutter Project
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  flutter_rating_bar: ^4.0.1

  cupertino_icons: ^1.0.8


  # Firebase
  firebase_core: ^2.30.0
  firebase_auth: ^4.19.0
  cloud_firestore: ^4.17.0
  firebase_storage: ^11.7.5

  # State management
  provider: ^6.1.2

  # Image upload
  image_picker: ^1.1.2

  # Utilities
  intl: ^0.19.0
  uuid: ^4.4.0

 update-code

  image_picker: ^1.0.7
  flutter_rating_bar: ^4.0.1
  fl_chart: ^1.1.1
  intl: ^0.20.2
 main
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Nếu có ảnh local thì mở phần này
  # assets:
  #   - assets/images/
